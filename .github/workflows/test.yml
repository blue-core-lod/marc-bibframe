name: Test
on:
  - push
  - pull_request
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12", "3.13"]
    steps:

      - name: checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Formatting
        run: uv run ruff format --diff

      - name: Linting
        run: uv run ruff check

      - name: Type Checking
        run: uv run ty check

      - name: Tests
        env:
          # The upstream-version test calls the GitHub API. Without a token the
          # anonymous limit is 60/hour per IP, which shared runners share, and
          # the test skips instead of checking anything.
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: uv run pytest

      - name: Vendored stylesheets match upstream plus patches
        run: |
          for pair in "marc2bibframe2:xsl" "bibframe2marc:bf2marc"; do
            stylesheet="${pair%%:*}"
            directory="src/marc_bibframe/${pair##*:}"
            tag="$(grep '^tag:' "$directory/UPSTREAM" | cut -d' ' -f2)"
            uv run ./scripts/vendor.py "$stylesheet" "$tag"
            git diff --exit-code -- "$directory"
          done
